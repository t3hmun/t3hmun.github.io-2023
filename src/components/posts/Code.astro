---
const props = Astro.props;
---

<!-- The _ in pre_code is tailwind's equivalent of a space, to make the decendant selector syntax. -->
<code
    {...props}
    class="rounded-lg pt-0.5 pb-0.5 pl-1 pr-1 font-mono [&:not(pre_code)]:bg-stone-200 dark:[&:not(pre_code)]:bg-stone-800"
    ><slot /></code
>
