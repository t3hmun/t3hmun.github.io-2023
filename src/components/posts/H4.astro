---
const props = Astro.props;
---

<h4
    {...props}
    class="mx-auto my-8 max-w-3xl pb-4 text-2xl font-semibold dark:text-amber-200"
>
    <a
        href={"#" + props.id}
        class="after:ml-2 after:text-stone-500 hover:after:content-['#']"
        ><slot /></a
    >
</h4>
